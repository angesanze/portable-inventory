# Development & test dependencies — NOT installed in the production image.
# Install locally with:  pip install -r requirements-dev.txt
-r requirements.txt

pytest
pytest-django
django-debug-toolbar
# Pinned so local `make lint`, the pre-commit hook and CI all format/lint
# identically (ruff formatting can differ between versions). Keep in sync with
# .pre-commit-config.yaml and backend/ruff.toml.
ruff==0.15.20
